# 运行时依赖。
# 每条都带上界:上界不是"这个大版本一定不兼容",而是"没在这个大版本上验证过"。
# 跨大版本升级请先跑 tests/ 再抬上界,不要让 pip 静默解析到未验证的大版本。
#
# 实测通过的版本(Python 3.11):
#   flask 3.1.3 / openai 2.43.0 / asyncssh 2.23.1 / watchdog 6.0.0

flask>=2.3,<4

# 忽略 jiter(直接跳过 jiter,不安装它),仅安装 openai 本体。
# 说明:jiter 是 openai 的可选/传递依赖,这里显式声明跳过。
# 若 openai 强依赖 jiter,pip 仍可能拉取,可用 --no-deps 精确控制,或改用下方约束方式。
# 推荐做法:直接安装 openai(不带依赖)并自行声明所需依赖。
openai>=1.0,<3
asyncssh>=2.13,<3
watchdog>=4.0,<7
