# 运行时依赖。
# 每条都带上界:上界不是"这个大版本一定不兼容",而是"没在这个大版本上验证过"。
# 跨大版本升级请先跑 tests/ 再抬上界,不要让 pip 静默解析到未验证的大版本。
#
# 实测通过的版本(Python 3.11):
#   flask 3.1.3 / asgiref 3.11.1 / openai 2.43.0 / asyncssh 2.23.1 / watchdog 6.0.0

flask>=2.3,<4
asgiref>=3.7,<4
openai>=1.0,<3
asyncssh>=2.13,<3
watchdog>=4.0,<7
